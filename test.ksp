(var                  # assign value to symbol
  current             # symbol to assign to
  (eval               # evaluate string as if it were a program
    (read "start: ")  # read input from stdin, prompt 'start: '
  )
)

(var end (eval (read "end: ")))

(while
  (<= current end)
  (
    (print current)
    (inc 'current) # Increment current
  )
)
