(def test (fn
  [x]
  (cond
    (> x 10) "Pretty big!"
    (< x 5) "Quite small"
    "Medium size")))

(println (test 40)) # => "Pretty big!"
(println (test 2)) # => "Quite small"
(println (test 6) )# => "Medium size"
